# 更新日志（中文）

本文件是 `CHANGELOG.md` 的中文对照版，内容一一对应，供审阅使用。
英文版是权威版本（发布说明从英文版整理），两份改动请一起维护。

## v1.3.0

- 手动导入取代了“监控临时文件夹”的做法。弹窗不再盯着一个文件夹，而是提供「浏览选择
  存档文件」（Tk 版是系统文件对话框，HTML 版是浏览器的文件选择器），并保留粘贴存档
  文本的输入框。选中的文件被读取并写入所选槽位，写入前先校验，且**不会**删除或改动
  你选的文件（旧行为会删掉用过的临时文件）。覆盖槽位前仍会把原存档备份到
  `kgsm_data/backups/`。整套候选/轮询状态机（候选重置、半成品下载后缀、两分钟放弃、
  目录快照）随之删除。
- 弹窗开始说明存档库：显示存档库文件夹并提供「复制路径」，同时说明存档归属哪个槽位
  由文件名（`名字_1.kgsav` … `名字_10.kgsav`）决定，因此可以在文件管理器里改名，再点
  「刷新」生效。
- 删除了临时文件夹 `kgsm_data/kgsm_temp`：不再创建、不在启动时清理、也不在异常检查里
  列出，因为手动导入已经改为选择文件。磁盘上已有的目录不会被删除。
- HTML 界面按参考设计（`OpenDesign/Suzuyuki Corridor`）重做：冰白底 + 单一雾蓝紫点缀，
  发丝线取代投影，衬线大标题配等宽大写眉题，浅色磨砂侧栏，导航是编号药丸（`01`…`06`，
  当前项用强调色填充），浅色等宽日志/代码面板，编号式引导列表，发丝线表格与悬停行。
  正文配色经过 WCAG AA 核算，实测 5.5:1 – 16.9:1（发丝线属于装饰，非文字）。
- 界面改为宽屏多列布局（≥1180px 两列、≥1720px 三列），内容区自己纵向滚动，卡片横向铺开
  而不是堆成一窄列。（后续调整：启动游戏、下载游戏、配置、修改存档页改回上下单列。）
- 页面标题去重：每页只保留一行小号双语眉题作为标题，只有首页保留衬线大标题，避免标题
  重复占用纵向空间。
- 应用窗口改为横向打开。Chromium 会按浏览器配置记住应用窗口的几何尺寸，所以启动参数
  `--window-size` 在打开过一次之后就被忽略；现在页面带 `fit=1` 自行调整窗口（实测
  1320×805，宽高比 1.64，居中于工作区），如果窗口已经够宽则不做改动。
- 页面把当前标签页写进地址栏，并支持 `?page=<kgsm|game|saves|editor|download|settings>`
  深链。
- 修掉两个界面问题：下载页两个复选框高低不齐（`.form label` 的选择器权重压过了
  `.check`，标签实际是带外边距的块级盒子；实测相差 8px，现已齐平并顶端对齐）；编辑器
  恢复了折叠功能——三角标可收起/展开节点，折叠状态按节点路径记录，改完数值重新渲染后
  仍然保持（根节点的 `path` 是 null，第一次实现因此点了没反应）。默认只展开根那一层
  （合成的 `(root)` 行与存档对象本身），其余容器全部收起。
- 编辑器工具栏对齐 Tk 版的排布：视图/源码是左侧的分段开关（有选中态），打开/写入在右侧。
- 按钮与药丸填充改用比文字强调色更浅的一档：文字仍是深色
  （`oklch(46% 0.105 262)`，用于描边与眉题），填充为 `oklch(56% 0.115 262)`
  （rgb(78,115,184)）配白字（4.69:1，达 WCAG AA）。深色当填充太重，而比这一档更浅的话
  白字就不合格了。
- 复选框行上下留白改为等距（各 16px，原来是上方 0、下方 22px，看起来像贴着上面）；下拉框
  改为自绘箭头并留出 38px 右侧内边距，浏览器原生箭头是贴边的。
- 弹窗里不该出现的「复制路径」按钮已修掉：`button { display: inline-flex }` 覆盖了
  `hidden` 属性，现在全局 `[hidden] { display: none !important }`。
- 侧栏左下角按钮改名为「退出」，退出的确认弹窗文案独立；下载页的取消按钮改名为「取消」。
  退出不再在页面日志里留下 `ERROR TypeError: Failed to fetch`（后端关闭服务导致响应中断，
  属正常），退出后应用窗口会自行关闭，普通标签页则显示“程序已退出，本页面可以关闭了”。
- 配置页改为改完即存：去掉「写入」按钮，「游戏目录」「固定端口」「浏览器」
  在失焦/变更后自动写入配置文件（与页面提示一致）。
- 配置页新增两项，**只作用于「启动游戏」打开的窗口**（程序自己的界面窗口固定是应用窗口，
  加载时自己调成横向并居中，不受这些设置影响）：
  **游戏窗口位置**（独立窗口（应用模式）/ 浏览器标签页），以及**自动续玩**开关。
- 新增自动续玩（默认开）：关掉游戏窗口/标签（含刷新）时，注入脚本把当时的存档交给程序，
  存成 `kgsm_data/session.kgsav`；下次打开游戏、页面一连上就自动灌回去并刷新，于是继续上次
  的进度。游戏自己的存档在浏览器里按来源（含端口）隔离，端口一变就是空的，所以这一步由程序
  保存，与端口无关。实测（假游戏页 + 真浏览器）：关窗口写出
  `LZ:{"saveVersion":2,...}`；先跑在 7483、换成 14770 再打开，页面标题显示已恢复成那份存档，
  运行日志留下 `[SESSION_LOAD] 已自动恢复上次进度`。关掉开关则既不保存也不恢复。
- 「启动游戏」此前总是用普通浏览器窗口打开（带标签栏与地址栏），配置了应用模式也没用；
  现在游戏页按上面的设置打开（应用窗口 / 浏览器标签页），标签页模式实测不新开窗口
  （窗口数不变，标题变成「… 及其它 1 个页面」）。
- 删掉了配置页与下载页的「浏览」按钮：HTML 版里它只是弹一个文本框让你粘贴路径，和直接在
  输入框里粘贴没有区别（浏览器不会把本地路径交给网页，用它选文件只能拿到文件名）。Tk 版
  的「浏览」是系统对话框，保留。
- 修正固定端口被自动端口覆盖：端口被占用时本次改用自动端口，但**不再把新端口写回配置**。
  此前那段代码把 `port_text` 清空之后按「没配端口」处理，于是用户设的固定端口被悄悄换掉；
  只要另有一份程序在跑（或端口被别的程序占用），每次启动都会换成新的随机端口——实测日志里
  一串「固定端口 N 已被占用，本次改用自动端口」就是这么来的。现在配置值保持不变，只有配置
  里本来就空着时才把自动端口记下来。
- 删除了「默认存档位」设置（两个版本都删）。它是为 Tk 版首页那个已被移除的功能做的配置
  入口，功能删掉之后只剩「启动时选中哪一格」这一个用途，而且在 HTML 版连消费者都没有：
  页面拿到 `home_slot` 只是用它填自己那个下拉框，存档列表的选中项一直是第 1 个（另有
  一个前端根本不读的 `default_slot` 字段）。现在配置、接口与翻译键一并删除；旧配置文件里
  遗留的 `home_slot` 会被忽略，下次保存时自动消失。
- 槽位显示格式统一为「存档NN-名字」（列表与下拉框一致，分隔符由「.」改为「-」），列表里
  不再附加文件名。
- 附带中文离线文档 `docs/guide_zh.html` 与中文更新日志 `CHANGELOG_zh.md`；程序里的
  「离线文档」「更新日志」入口会按界面语言打开对应语言的版本。
- 端口：没有配置固定端口时，自动分配到的端口会写入配置并在之后沿用（早期版本的行为）。
  端口被占用不再导致下次启动失败：先探测端口是否空闲（Windows 的 `SO_REUSEADDR` 允许
  绑定已占用端口，只看绑定是否报错判断不出来），占用时退回自动端口并在启动日志里告警。
  （写回的规则后来修正为「只有配置里空着时才写」，见上面「修正固定端口被自动端口覆盖」。）
- 移植 v1.2.3 的修复到本条线（只修 bug，不加功能）：默认分支改为可靠探测而不是猜成
  `main`（作者原版仓库只有 `master`，猜错会导致 GitHub API 限流时所有下载源 404）；版本
  列表缓存十分钟，只有点按钮才重新抓取；下载日志会说明回退原因。Tk 版界面回调里的
  未处理异常不再静默（写运行日志、页面日志并弹窗给出日志路径），退出时取消排队中的轮询
  任务。
- 移植 v1.2.4 的修复到本条线（只修 bug，不加功能）：存档载荷不再被整体 strip（UTF-16
  存档尾部的空格是载荷本身，只裁行尾换行，编解码器同时接受原始与裁掉行尾的文本）；手动
  导入不再卡死（忽略半成品下载、候选消失或长时间不变时重扫）；覆盖槽位前备份。
- 桥的两处修复：已有请求在途时，新的 `request_save` 会等待而不是立刻失败（旧行为会误报
  超时/解码失败），并且尊重调用方给的超时；`apply_save` 会等待页面确认写入，区分“已确认/
  页面报错/已发送但未确认”，不再默认成功。编辑器的实时取档、实时写入以及把槽位载入游戏
  都改到后台线程，两个前端都不再冻结窗口。
- 下载页版本列表的缓存日志不再把来源打印在默认分支的位置。下载器缓存备注会报告缓存的
  默认分支。
- 两个前端都带上程序图标：可执行文件使用 `assets/KGSaveManager.ico`，Tk 窗口与其对话框
  使用同一图标，HTML 版把它作为 favicon（`webapp/assets/favicon.ico` 及由它生成的 PNG）。
- HTML 前端现在是主版本并保留 `KGSaveManager.py` 这个名字；Tkinter 前端称为 Tk 版
  （`KGSaveManagerTk.py`，打包为 `KGSaveManagerTk.exe`）。两者共用 `kgsm_data/`，
  行为一致。Tkinter 前端在 v1.3.0 开发期曾叫 Lite 版；因为「便携」对两个前端同样成立、
  真正的区别是界面技术，所以改成了现在的名字。
- HTML 界面：整个界面在系统浏览器（Edge/Chrome 应用窗口）里渲染，通过本地 HTTP/JSON
  接口与同一套逻辑层通信，不依赖任何第三方库。
- 分层结构：`core/` 存放全部与界面无关的逻辑（路径、配置、日志、槽位、存档流程、
  服务与桥的生命周期、下载控制、存档编辑器），只通过 `core.ui_port.UiPort` 与屏幕交互；
  `ui_tk.py` 实现 Tkinter 端口，`webapp/` 实现浏览器端口。
- Tkinter 各页（`pages_manual.py`、`pages_editor.py`、`pages_download.py`）只做视图：
  建控件、调 `core`、渲染事件。`save_flows.py` 被 `core/flows.py` 加 `pages_manual.py`
  取代。
- Web UI：页面导航、带实时日志的启动页、带槽位备注的存档管理、手动导入（选文件或粘贴
  文本）、存档编辑器（树与 JSON 源码）、带进度与连通性测试的游戏下载器、配置页，
  以及由后端推送的中英文字符串。
- HTML 前端自带自检（`?selftest=1`），测试套件用无头 Edge 驱动它，因此浏览器界面是端到端
  验证过的。
- 文档增加中文版：`docs/guide_zh.html`（结构与锚点与英文版一一对应）与
  `CHANGELOG_zh.md`（按版本段对应本文件）。程序里的入口会按语言选择：“关于”板块提供
  「离线文档」与「更新日志」，Tk 版的文档按钮在中文界面下打开中文指南。两份更新日志
  都由 PyInstaller spec 打包分发。
- 退出不再留下报错。此前页面会等待 `shutdown` 的响应，而服务已经关闭，于是日志里出现
  `ERROR TypeError: Failed to fetch`；现在请求改为不等回包，应用窗口会自行关闭，普通
  浏览器标签页则显示“程序已退出，本页面可以关闭了”。
- 弹窗里本来该隐藏的按钮显示了出来：退出确认框里出现了「复制路径」，原因是
  `button { display: inline-flex }` 的选择器权重压过了 `hidden` 属性。现在全局
  `[hidden] { display: none !important }`，对所有元素生效。
- 槽位显示处处统一为「存档NN-名字」（英文界面 `Save NN-name`，分隔符由「.」改为「-」），
  存档列表也不再附加文件名。
- 离线文档跟随界面语言，也可以用外部翻译替换：“关于”在中文界面打开
  `docs/guide_zh.html`，否则打开 `docs/guide_en.html`（更新日志同理取
  `CHANGELOG_zh.md` / `CHANGELOG.md`）；把同名文件放进 `i18n/` 会优先于随包文档，
  因此可以不改程序就换成自己的翻译版。Tk 版此前把文档链接写死成英文文件名，已修正。
- 文档入口只在首页显示；配置页末尾改为「数据目录」卡片，不再重复一个「关于」板块。
- Tk 版修改存档页修复：启动后槽位下拉是空的，导致即使存档库里有存档，点「打开」也
  只会提示「请先选择一个有存档的槽位」——现在建好控件就填充下拉；另外「视图/源码」那一行
  不再被拉高（原本两行都在争抢多余高度，上下各留出约 66px 空白，现在多余高度全部给内容区）。
- 主版本不再自行退出。Chromium 会把隐藏页面里的定时器降频到约每分钟一次，所以「页面好一会儿
  没消息」无法区分用户离开和窗口被最小化/被遮住：12 秒的看门狗先改为 180 秒，现在看门狗、
  它依赖的心跳接口 `/api/ping` 以及配套的 `--serve` 参数一并删除。退出只有两条路——页面里的
  「退出」按钮（关闭窗口也会触发）和控制台的 Ctrl+C。
- 修改存档页：槽位下拉会保留你选的那一项。下拉内容每次刷新都由后端重画，此前每点一次
  「打开」就会跳回第一项。切到实时模式时下拉被禁用，并说明实时模式直接读写运行中的游戏、
  不使用存档槽位。
- 新增合并版设计系统参考（在仓库外）：`Web_project/kgsm-design-system/` —— 以本界面现有样式
  为底，补上参考设计有而本项目没用到的部分（标签、状态徽标、时间线行、占位图、空态、雪线
  分隔、星光氛围、页脚、顶部毛玻璃导航、抽屉、浮起卡片、滚动显现、网格原语），包含展示页、
  分层 CSS（`tokens.css` / `base.css` / `components.css`）、机器可读的 `tokens.json`，
  以及 `README.md` / `AGENTS.md` / `SOURCES.md`。
- HTML 界面按钮填充恢复原版色相并提亮一档，不再使用偏青的版本；自检会核对填充与文字
  这一对颜色、侧栏当前项的药丸配色，以及编辑器下拉的行为。
- 测试：208 项，覆盖 core（槽位、流程、编辑器、下载、服务）、桥协议与保活、注入脚本、
  下载器、Python 与 web 资源两侧的 i18n、死代码检查，以及浏览器自检。/ 测试只用标准库，
  不访问网络、不启动真实浏览器。静态检查会在遗留未使用的 import、未被引用的函数或没有
  引用的翻译键时让测试失败。
- 说明：曾评估用 pywebview 做内嵌窗口方案并放弃——pywebview 6.2.1 配 pythonnet 3.1.0 在
  Python 3.14 上会以 `AccessibilityObject.Bounds` 递归错误挂死。基于浏览器的前端则完全
  不需要第三方依赖。

## v1.2.4

这个版本只发布了 Tkinter 前端（HTML 前端从 v1.3.0 开始提供），从 `release/1.2.x`
分支发布；修复已回迁到 v1.3.0 线，见上面「移植 v1.2.4 的修复」条目。

- 程序自带图标：可执行文件在资源管理器与任务栏显示该图标，主窗口与其对话框运行时使用
  同一图标。
- 手动存档不再永久卡死：半成品文件（例如浏览器的 `.crdownload`）被选为候选后又被改名或
  删除时，弹窗会一直轮询那个路径，此后再也导入不了任何东西。现在候选消失、或两分钟内
  始终在变化时会重置并重扫，且彻底忽略半成品后缀（`.crdownload`、`.part`、`.tmp` 等）。
- 手动存档支持「先导出文件、再点手动存档」这一自然顺序：临时文件夹里已有的文件会被选为
  候选（下载日志会说明）。
- 导入不再改动存档内容：只裁掉行尾换行，而不是整体 strip。UTF-16 存档尾部的填充空格属于
  数据本身，旧行为既改了文件，又让格式检查把完全正常的存档报成“可能无效”。编解码器现在
  先识别格式（JSON / Base64 / UTF-16），解码时容忍行尾填充被裁掉，与游戏的 JavaScript
  实现一致。
- 自动读档与编辑器实时写入改为等待页面确认结果（`apply_ok` / `apply_err`），不再一发出去
  就报成功；页面超时未确认时会说明，而不是谎报成功。两者都在后台线程执行，窗口不再卡住。
- 编辑器的「从运行中的游戏打开」不再冻结窗口：拉取在后台执行，结果到达后再应用。
- 手动存档与自动存档覆盖槽位前会先备份到 `kgsm_data/backups/`，命名与编辑器一致。
- 自动分配的端口不再写入配置：过去它被当成固定端口存下来，于是下次启动只要该端口被占用
  就会失败。
- 桥的并发请求不再立刻失败：在途请求存在时，新请求会等待而不是立刻返回“无数据”，并且
  尊重调用方给的超时。
- 版本列表的缓存日志不再把来源打印在默认分支的位置。
- 测试：新增 `tests/test_manual_save.py`（手动存档状态机），并扩展桥的测试（写入确认、
  并发请求）。

## v1.2.3

这个版本只发布了 Tkinter 前端，从 `release/1.2.x` 分支发布；修复已回迁到 v1.3.0 线，
见上面「移植 v1.2.3 的修复」条目。

- 修正下载页面对作者原版仓库（`nuclear-unicorn/kittensgame`）全部源都 404、而社区仓库
  正常的问题。此前只要 GitHub API 调用失败就把默认分支猜成 `main`，而作者原版仓库只有
  `master`，于是四个源全部 404。现在改为可靠探测：先查 API；API 不可用时（未认证的
  GitHub API 每 IP 每小时限制 60 次）逐个比对压缩包地址，取真正存在的分支。下载日志会
  说明原因（例如“GitHub API 不可用（可能被限流），探测到默认分支：master”）。
- 版本列表缓存十分钟，只有点「刷新版本」按钮才重新抓取，浏览页面或切换语言不再消耗 API
  配额。
- 修正下载页点「下载」没有任何反应：版本列表把仓库 ref 存成字符串，而启动流程按
  `(kind, ref)` 解包，于是每次点击都在 Tkinter 回调里抛
  `ValueError: too many values to unpack`。打包后的窗口版没有控制台，这个错误完全静默。
  现在按钮能正常开始下载。（自 v1.2.0 起就是坏的。）
- 界面回调里的未处理异常不再静默：会写入运行日志与页面日志，并弹窗给出日志路径。
- 下载线程不再从后台线程操作 Tkinter 变量（写入游戏目录、同步输入框改为在 `dl_done`
  事件后于界面线程执行），此前可能抛 `main thread is not in main loop`。
- 退出时取消排队中的界面轮询任务，避免关闭时出现 Tcl 报错。
- 新增 `tests/test_download_page.py`：用打桩的下载器驱动下载页，如果按钮以后又不启动
  下载就会失败。

## v1.2.2

- 修正桥的诊断：游戏启动前 `<div id="game">` 已经作为 `window.game` 暴露，曾被误判为
  “引擎存在但没有 `save()`”。现在会把 DOM 节点排除在引擎候选之外，启动期间重复扫描引擎，
  引擎就绪后重新发送 `hello`，日志里显示的是真实引擎状态。
- 实时取档会先等待引擎（最多五秒）再退回浏览器自动保存快照，服务日志会标注数据来源
  （`engine` 或 `localStorage`）。
- 空闲的桥连接改用 WebSocket ping 帧保活，不再在 30 秒后关闭；不再回应 pong 的连接会在
  75 秒后回收。启动日志里周期性的断开/重连因此消失。
- 手动存档：同名文件被覆盖且内容不同时现在也能检测到（此前只检测全新的文件名）；导入
  成功后删除用过的临时文件；启动时清理临时文件夹里超过七天的文件。
- 镜像列表修正：`ghproxy.net` 一项原本指向无关的 `mirror.ghproxy.com` 域名，该域名已无
  响应。
- 删除死代码：改版前的首页动作（`kgm_quick_run`、`kgm_copy_run`、`_resolve_kgm_save`、
  `_refresh_kgm_dir`、`_refresh_kgm_save`、`kgm_mode_var`、`open_download_tab`、
  `MONITOR_TIMEOUT`、`GAME_DOWNLOAD_URLS`、未使用的 JSON 辅助函数与 import）。
  `msg.monitor_timeout` 等 38 个没有引用的翻译键一并删除。
- “Home Save Slot”改名为“Default Save Slot”（首页改版后它已经没有使用点，现在用于选择
  启动时默认选中的槽位）。
- 存档编解码：压缩改为按 UTF-16 码元处理，与 JS 原版一致，含非 BMP 字符（emoji）的存档
  往返不再损坏。
- 新增 `tests/` 下的测试（只用标准库）：存档编解码往返与和游戏自带 `lib/lz-string.js`
  的一致性、下载器解压、桥协议与保活、注入脚本场景、槽位写入、翻译表，以及针对未使用
  import、未被引用函数、未使用翻译键的静态检查。

## v1.2.1

- 存档桥：引擎发现扩展到 window 键与 `gamePage`；增加 LZString 压缩回退；实时取档不再只
  依赖 `window.game`。
- 桥诊断：启动页的服务日志会显示连接/断开与握手信息。

## v1.2.0

- 存档编辑器：打开槽位文件前，先在 `kgsm_data/backups/` 写一份带日期的备份
  `名字.kgsav.YYYYMMDD_HHMMSS.bak`。
- 首页（KGSM）改版：左侧是各页面按钮，右侧是上手流程（读文档、下载、配置、启动），
  保留页面标题。
- 外部翻译：启动时加载 `i18n/` 下的 JSON 与 PO 文件；文档里说明了格式与键前缀。
- 编辑器视图：数组元素带下标，元素是对象时附上第一个键值。
- 保存时规范化路径（去掉引号，统一反斜杠）。
- 运行日志系统：`kgsm_data/kgsm_log/<启动时间>.log`，每次启动一个文件。
- 新增英文离线文档 `docs/guide_en.html`，并随可执行文件一起打包。
- 文档措辞改为平实的技术表述。

## v1.1.0

- 日志与对话框实现完整本地化（中/英）。
- README 重组：英文为主文件，中文单独一份。
- PyInstaller spec 显式声明模块；构建验证通过。
- 槽位识别改为读取存档库文件名；配置只保存备注。
- 新增存档编辑器：解码、编辑（视图/源码）、重新编码写回；为写回移植了 lz-string 压缩。
- 通过本地 WebSocket 桥实现自动存档与自动读档；手动存档对话框（导入文件或粘贴文本并
  校验）。
- 下载游戏页：仓库、版本、镜像、连通性测试、解压摊平。
- 代码按模块拆分，页面使用 mixin。

## v1.0.0

- 首个版本：标签页界面（KGSM、启动游戏、存档管理、配置）、10 个存档位、备注持久化、
  本地 Web 服务、中英双语界面。

