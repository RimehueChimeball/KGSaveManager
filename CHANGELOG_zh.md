# 更新日志（中文）

本文件是 `CHANGELOG.md` 的中文对照版，内容一一对应，供审阅使用。
英文版是权威版本（发布说明从英文版整理），两份改动请一起维护。

## v1.3.0

相对 v1.2.4（当时只有 Tkinter 界面）的变化。

**新增：HTML 界面（现在是主版本）**

- 新增 HTML 前端并成为主版本，入口仍是 `KGSaveManager.py`（后端 `webapp/`，界面资源
  `webapp/assets/`）：程序在 `127.0.0.1` 上提供界面，并以浏览器应用窗口打开（Edge/Chrome 的
  `--app=`，无地址栏与标签页），窗口尺寸、标题与图标由页面提供。Tkinter 前端改名为 Tk 版
  （`KGSaveManagerTk.py`）继续提供，两者共用 `core/` 与同一份 `kgsm_data/`。
- 六个页面：KGSM、启动游戏（实时日志）、存档管理（10 个槽位与备注）、修改存档（视图/源码、
  可折叠）、下载游戏、配置。地址支持 `?page=…` 深链，加 `?selftest=1` 可跑页面自检。
- 界面风格：冰白底 + 单一雾蓝紫点缀，发丝线取代投影，衬线大标题配等宽大写眉题，编号药丸导航。
  正文对比度按 WCAG AA 核算（实测 5.5:1 – 16.9:1）；填充色与文字是固定搭配
  （`--accent-fill` 配白字，4.69:1）。布局以单列页面为主，内容区自己纵向滚动，卡片网格用
  `minmax(0, 1fr)` 防止内容撑破容器。
- 配置页只保留真正有用的项：语言、浏览器、游戏目录、固定端口、游戏窗口位置（独立窗口
  （应用模式）/ 浏览器标签页）与自动续玩；改动即存，没有「写入」按钮。
- 自动续玩（默认开）：关掉游戏窗口或标签（刷新也算）时，把当时的进度存成
  `kgsm_data/session.kgsav`；下次打开游戏、页面一连上就自动恢复，所以与端口无关——游戏自己的
  存档在浏览器里按来源（含端口）隔离，换端口就等于空档。游戏里删档/重置时，这份进度会一并清除。
- 游戏窗口位置只作用于「启动游戏」打开的窗口；程序界面窗口固定是应用窗口，加载时自己调成横向
  并居中。游戏窗口初始尺寸与界面窗口相同，之后沿用浏览器记住的尺寸。
- HTML 版不提供「浏览」按钮：浏览器不会把本地路径交给网页（选文件只能拿到文件名），所以路径
  直接输入或粘贴；需要选存档文件的手动导入用的是浏览器自带的文件选择器。Tk 版仍是系统对话框。
- 退出只有两条路：页面里的「退出」按钮（关窗口同样触发）与控制台 Ctrl+C；没有基于页面心跳的
  自动退出（浏览器会把隐藏页面的定时器降频到约每分钟一次，「没有联系」无法与「用户离开了」区分）。

**分层结构**

- `core/` 收纳全部与界面无关的逻辑（路径、配置、日志、槽位、存档流程、服务与桥的生命周期、
  下载、编辑器），只通过 `core/ui_port.UiPort` 与屏幕交互；`ui_tk.py` 与 `webapp/` 各自实现这个
  端口，页面代码只负责建控件、调 `core`、渲染事件。

**存档流程**

- 手动存档改为显式导入：选择文件（Tk 用系统对话框，HTML 用浏览器文件选择器）或粘贴存档文本；
  不再监控临时文件夹，`kgsm_data/kgsm_temp` 不再创建或清理，导入也不会删改你选的文件；覆盖槽位前
  仍会备份到 `kgsm_data/backups/`。
- 端口：配置里留空时，本次自动分配到的端口会记下来并在之后沿用；配置里已有端口而被占用时，
  本次退回自动端口，**配置里的端口保持不变**（不再用自动端口覆盖你设的值）。
- 回迁 v1.2.3 与 v1.2.4 的修复：默认分支探测、版本列表缓存十分钟、桥的并发请求与写入确认、
  存档载荷不再被整体 strip、手动导入不再卡死、覆盖前备份等——每条在上面 v1.2.3 / v1.2.4
  版本段里有完整说明；这两个版本只有 Tkinter 界面。

**界面细节**

- 下载页两个复选框与文字首行对齐；下拉框自绘箭头并留出右侧内边距；槽位显示统一为
  「存档NN-名字」；弹窗里不该出现的「复制路径」按钮已修掉；编辑器工具条与 Tk 版一致（视图/源码
  在左，打开/写入在右），默认只展开根那一层，实时模式下槽位下拉置灰。

**文档与测试**

- 附带中英双语离线文档（`docs/guide_en.html`、`docs/guide_zh.html`）与中文更新日志
  `CHANGELOG_zh.md`；程序里的入口按界面语言选择，把同名文件放进 `i18n/` 可以换成自己的翻译。
- 测试 220 项，只用标准库：存档编解码（有 Node.js 时与游戏自带 `lib/lz-string.js` 对照）、槽位
  扫描、存档流程、编辑器、下载器、服务与 WebSocket 桥（含注入脚本的 node 夹具）、i18n 两侧一致性
  与死代码检查；装了 Edge/Chrome 时还会用无头浏览器跑一遍 HTML 界面的自测。

**打包**

- 两个 PyInstaller 配置：`KGSaveManager.spec`（主版本，含 `webapp/assets`）与
  `KGSaveManagerTk.spec`（Tk 版），产物都是 onedir。

## v1.2.4

这个版本只发布了 Tkinter 前端（HTML 前端从 v1.3.0 开始提供），从 `release/1.2.x`
分支发布；修复已回迁到 v1.3.0 线，见上面「移植 v1.2.4 的修复」条目。

- 程序自带图标：可执行文件在资源管理器与任务栏显示该图标，主窗口与其对话框运行时使用
  同一图标。
- 手动存档不再永久卡死：半成品文件（例如浏览器的 `.crdownload`）被选为候选后又被改名或
  删除时，弹窗会一直轮询那个路径，此后再也导入不了任何东西。现在候选消失、或两分钟内
  始终在变化时会重置并重扫，且彻底忽略半成品后缀（`.crdownload`、`.part`、`.tmp` 等）。
- 手动存档支持「先导出文件、再点手动存档」这一自然顺序：临时文件夹里已有的文件会被选为
  候选（下载日志会说明）。
- 导入不再改动存档内容：只裁掉行尾换行，而不是整体 strip。UTF-16 存档尾部的填充空格属于
  数据本身，旧行为既改了文件，又让格式检查把完全正常的存档报成“可能无效”。编解码器现在
  先识别格式（JSON / Base64 / UTF-16），解码时容忍行尾填充被裁掉，与游戏的 JavaScript
  实现一致。
- 自动读档与编辑器实时写入改为等待页面确认结果（`apply_ok` / `apply_err`），不再一发出去
  就报成功；页面超时未确认时会说明，而不是谎报成功。两者都在后台线程执行，窗口不再卡住。
- 编辑器的「从运行中的游戏打开」不再冻结窗口：拉取在后台执行，结果到达后再应用。
- 手动存档与自动存档覆盖槽位前会先备份到 `kgsm_data/backups/`，命名与编辑器一致。
- 自动分配的端口不再写入配置：过去它被当成固定端口存下来，于是下次启动只要该端口被占用
  就会失败。
- 桥的并发请求不再立刻失败：在途请求存在时，新请求会等待而不是立刻返回“无数据”，并且
  尊重调用方给的超时。
- 版本列表的缓存日志不再把来源打印在默认分支的位置。
- 测试：新增 `tests/test_manual_save.py`（手动存档状态机），并扩展桥的测试（写入确认、
  并发请求）。

## v1.2.3

这个版本只发布了 Tkinter 前端，从 `release/1.2.x` 分支发布；修复已回迁到 v1.3.0 线，
见上面「移植 v1.2.3 的修复」条目。

- 修正下载页面对作者原版仓库（`nuclear-unicorn/kittensgame`）全部源都 404、而社区仓库
  正常的问题。此前只要 GitHub API 调用失败就把默认分支猜成 `main`，而作者原版仓库只有
  `master`，于是四个源全部 404。现在改为可靠探测：先查 API；API 不可用时（未认证的
  GitHub API 每 IP 每小时限制 60 次）逐个比对压缩包地址，取真正存在的分支。下载日志会
  说明原因（例如“GitHub API 不可用（可能被限流），探测到默认分支：master”）。
- 版本列表缓存十分钟，只有点「刷新版本」按钮才重新抓取，浏览页面或切换语言不再消耗 API
  配额。
- 修正下载页点「下载」没有任何反应：版本列表把仓库 ref 存成字符串，而启动流程按
  `(kind, ref)` 解包，于是每次点击都在 Tkinter 回调里抛
  `ValueError: too many values to unpack`。打包后的窗口版没有控制台，这个错误完全静默。
  现在按钮能正常开始下载。（自 v1.2.0 起就是坏的。）
- 界面回调里的未处理异常不再静默：会写入运行日志与页面日志，并弹窗给出日志路径。
- 下载线程不再从后台线程操作 Tkinter 变量（写入游戏目录、同步输入框改为在 `dl_done`
  事件后于界面线程执行），此前可能抛 `main thread is not in main loop`。
- 退出时取消排队中的界面轮询任务，避免关闭时出现 Tcl 报错。
- 新增 `tests/test_download_page.py`：用打桩的下载器驱动下载页，如果按钮以后又不启动
  下载就会失败。

## v1.2.2

- 修正桥的诊断：游戏启动前 `<div id="game">` 已经作为 `window.game` 暴露，曾被误判为
  “引擎存在但没有 `save()`”。现在会把 DOM 节点排除在引擎候选之外，启动期间重复扫描引擎，
  引擎就绪后重新发送 `hello`，日志里显示的是真实引擎状态。
- 实时取档会先等待引擎（最多五秒）再退回浏览器自动保存快照，服务日志会标注数据来源
  （`engine` 或 `localStorage`）。
- 空闲的桥连接改用 WebSocket ping 帧保活，不再在 30 秒后关闭；不再回应 pong 的连接会在
  75 秒后回收。启动日志里周期性的断开/重连因此消失。
- 手动存档：同名文件被覆盖且内容不同时现在也能检测到（此前只检测全新的文件名）；导入
  成功后删除用过的临时文件；启动时清理临时文件夹里超过七天的文件。
- 镜像列表修正：`ghproxy.net` 一项原本指向无关的 `mirror.ghproxy.com` 域名，该域名已无
  响应。
- 删除死代码：改版前的首页动作（`kgm_quick_run`、`kgm_copy_run`、`_resolve_kgm_save`、
  `_refresh_kgm_dir`、`_refresh_kgm_save`、`kgm_mode_var`、`open_download_tab`、
  `MONITOR_TIMEOUT`、`GAME_DOWNLOAD_URLS`、未使用的 JSON 辅助函数与 import）。
  `msg.monitor_timeout` 等 38 个没有引用的翻译键一并删除。
- “Home Save Slot”改名为“Default Save Slot”（首页改版后它已经没有使用点，现在用于选择
  启动时默认选中的槽位）。
- 存档编解码：压缩改为按 UTF-16 码元处理，与 JS 原版一致，含非 BMP 字符（emoji）的存档
  往返不再损坏。
- 新增 `tests/` 下的测试（只用标准库）：存档编解码往返与和游戏自带 `lib/lz-string.js`
  的一致性、下载器解压、桥协议与保活、注入脚本场景、槽位写入、翻译表，以及针对未使用
  import、未被引用函数、未使用翻译键的静态检查。

## v1.2.1

- 存档桥：引擎发现扩展到 window 键与 `gamePage`；增加 LZString 压缩回退；实时取档不再只
  依赖 `window.game`。
- 桥诊断：启动页的服务日志会显示连接/断开与握手信息。

## v1.2.0

- 存档编辑器：打开槽位文件前，先在 `kgsm_data/backups/` 写一份带日期的备份
  `名字.kgsav.YYYYMMDD_HHMMSS.bak`。
- 首页（KGSM）改版：左侧是各页面按钮，右侧是上手流程（读文档、下载、配置、启动），
  保留页面标题。
- 外部翻译：启动时加载 `i18n/` 下的 JSON 与 PO 文件；文档里说明了格式与键前缀。
- 编辑器视图：数组元素带下标，元素是对象时附上第一个键值。
- 保存时规范化路径（去掉引号，统一反斜杠）。
- 运行日志系统：`kgsm_data/kgsm_log/<启动时间>.log`，每次启动一个文件。
- 新增英文离线文档 `docs/guide_en.html`，并随可执行文件一起打包。
- 文档措辞改为平实的技术表述。

## v1.1.0

- 日志与对话框实现完整本地化（中/英）。
- README 重组：英文为主文件，中文单独一份。
- PyInstaller spec 显式声明模块；构建验证通过。
- 槽位识别改为读取存档库文件名；配置只保存备注。
- 新增存档编辑器：解码、编辑（视图/源码）、重新编码写回；为写回移植了 lz-string 压缩。
- 通过本地 WebSocket 桥实现自动存档与自动读档；手动存档对话框（导入文件或粘贴文本并
  校验）。
- 下载游戏页：仓库、版本、镜像、连通性测试、解压摊平。
- 代码按模块拆分，页面使用 mixin。

## v1.0.0

- 首个版本：标签页界面（KGSM、启动游戏、存档管理、配置）、10 个存档位、备注持久化、
  本地 Web 服务、中英双语界面。

